<Window x:Class="ImageTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:GeneralTool.General.WPFHelper.WPFControls;assembly=GeneralTool.General"
        xmlns:local="clr-namespace:ImageTest"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Menu>
            <MenuItem Header="Load"
                      Click="MenuItem_Click" />
        </Menu>

        <c:ImageViewControl x:Name="IVC"
                            Grid.Row="1"
                            CutImageDownEvent="IVC_CutImageDownEvent"
                            CutPanelToolTip="AaA"
                            DoubleClickRaiseImage="True">

        </c:ImageViewControl>

        <StackPanel Grid.Row="2">
            <TextBlock x:Name="Txt" />
        </StackPanel>
    </Grid>
</Window>
